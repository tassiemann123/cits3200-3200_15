# Optional reference models

Place the optional model files in this directory using these exact names:

- `skeleton_pre-cut.glb`
- `female_skeleton.glb`

The application still builds and runs in landmark-only mode when these files are absent. The GLB files are excluded from Git until their redistribution rights have been confirmed.
